package library
fun main() {

    // while

        // when 관리자 모드
        // input = (id, pwd)
        // var mg = Manager class (id, pwd)
        // true mg.startIfRegistered(), false -> 다시 시도 or exit
        // while (!logout, !programexit)
            // 1) checkBookList () // 전체 도서 정보 및 대여 상황 보기
            // 2) updateBookCheckOutStatus() //대여 및 반납
            // 3) purchase()
                // addInBookList()
                // updateBalance()
            // 4) log out or programexit

        // when 멤버 모드
        // input = (id, pwd)
        // var mb = Member class (id, pwd)
        // true mb.startIfRegistered(), false -> 다시 시도 or exit
        // while (!logout, !programexit)
            // 1) printMyCheckOutStatus()
            // 2) borrowBook()
            // 3) returnBook()
            // 4) searchBook()
                // byName()
                // byAuthor()
            // 5) log out or programexit











    // 멤버 모드


}